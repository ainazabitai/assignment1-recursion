Abitay Ainaz IT-2502 
<img width="1014" height="873" alt="image" src="https://github.com/user-attachments/assets/dbe0eaad-8b2d-4445-9431-01f97450497d" />
in task 1 i used recursion.
First, I take the input as a string using Scanner.
Then I created a recursive function with an index parameter.
In each step, the function prints one digit using charAt(i)
and calls itself with the next index i + 1.
The base case is when the index reaches the length of the string,
so the recursion stops.
<img width="1014" height="873" alt="image" src="https://github.com/user-attachments/assets/6a1d5383-4307-48fb-834c-bbd91d2812ab" />
Task 2. I wrote a recursive method averageofElements that takes an array and its size,
and returns the sum of all elements. In main, I read n numbers from the user into an array, 
called the recursive method to get the total sum, and then calculated the average by dividing the sum by n. Finally, I printed the average.
<img width="1014" height="873" alt="image" src="https://github.com/user-attachments/assets/bd94b869-e2d0-4225-a19f-73b055489b2b" />
Task 3. I implemented a recursive method to check whether a number is prime. The function tests divisibility starting from 2 up to √n. The result ("Prime" or "Composite") is printed.
<img width="941" height="873" alt="image" src="https://github.com/user-attachments/assets/676e937f-8093-43e2-b44e-7c4d412be578" />
Task 4. I wrote a recursive function to compute the factorial of a number. The method multiplies n by factorial(n−1) until reaching the base case. The result is printed in main.
<img width="1014" height="873" alt="image" src="https://github.com/user-attachments/assets/b9b42b75-a9a8-4c43-aa57-d7984a25ab6e" />
Task 5. I implemented a recursive method to find the nth Fibonacci number. The function returns the sum of the two previous values. The result is displayed after user input.
<img width="1178" height="873" alt="image" src="https://github.com/user-attachments/assets/84d5ac3f-70fc-4bb5-96ee-ac9ccb54b480" />
Task 6. I created a recursive function to calculate a number raised to a power. The method multiplies the base repeatedly until the exponent reaches zero. The result is printed.
<img width="1324" height="873" alt="image" src="https://github.com/user-attachments/assets/f1f0af6a-0af5-4639-8997-6e8ce1881808" />
Task 7. I implemented a recursive method to print numbers in reverse order. The input is read first, and printing happens during the return phase of recursion. This produces reversed output.
<img width="1414" height="873" alt="image" src="https://github.com/user-attachments/assets/06f19f6f-8001-4a70-87b2-1904c5db698e" />
Task 8. I wrote a recursive function to verify if a string contains only digits. The method checks each character one by one. It returns "Yes" or "No" accordingly.
<img width="1540" height="873" alt="image" src="https://github.com/user-attachments/assets/a99a3839-af08-420b-bd7c-eaa3e03476d3" />
Task 9. I implemented a recursive method to count the number of characters in a string. Each recursive call processes one character. The total count is printed.
<img width="1540" height="873" alt="image" src="https://github.com/user-attachments/assets/e4bf3f73-f356-4092-bec4-3fb6a6090ae0" />
Task 10. I implemented the Euclidean algorithm using recursion to find the GCD of two numbers. The function repeatedly applies a % b until b becomes zero. The result is printed.
