<img width="1324" height="873" alt="image" src="https://github.com/user-attachments/assets/25bc55f6-b551-4278-9996-49c1a9a224e8" />Abitay Ainaz IT-2502 
<img width="1014" height="873" alt="image" src="https://github.com/user-attachments/assets/dbe0eaad-8b2d-4445-9431-01f97450497d" />
in task 1 i used recursion.
First, I take the input as a string using Scanner.
Then I created a recursive function with an index parameter.
In each step, the function prints one digit using charAt(i)
and calls itself with the next index i + 1.
The base case is when the index reaches the length of the string,
so the recursion stops.
<img width="1014" height="873" alt="image" src="https://github.com/user-attachments/assets/6a1d5383-4307-48fb-834c-bbd91d2812ab" />
I wrote a recursive method averageofElements that takes an array and its size,
and returns the sum of all elements. In main, I read n numbers from the user into an array, 
called the recursive method to get the total sum, and then calculated the average by dividing the sum by n.
Finally, I printed the average.
<img width="1014" height="873" alt="image" src="https://github.com/user-attachments/assets/bd94b869-e2d0-4225-a19f-73b055489b2b" />
I wrote a recursive method isPrime that takes a number and a divisor, and checks if the number is prime. In main,
I read a number from the user, called the recursive method starting with divisor 2, and printed whether the number is "Prime" or "Composite".
<img width="1014" height="873" alt="image" src="https://github.com/user-attachments/assets/b9b42b75-a9a8-4c43-aa57-d7984a25ab6e" />
I wrote a recursive method fibonacci that returns the nth Fibonacci number. In main, I read a number from the user,
called the recursive method, and printed the result.
<img width="1178" height="873" alt="image" src="https://github.com/user-attachments/assets/84d5ac3f-70fc-4bb5-96ee-ac9ccb54b480" />
I implemented a recursive function power to compute a number raised to a given exponent. In the main method,
I took the base and exponent as input from the user, invoked the function, and displayed the result.
<img width="1324" height="873" alt="image" src="https://github.com/user-attachments/assets/f1f0af6a-0af5-4639-8997-6e8ce1881808" />
I implemented a recursive method reverseOutput that reads a sequence of numbers and prints them in reverse order. In main, 
I read the number of elements from the user, then used the recursive function to input and display the numbers in reverse.
<img width="1414" height="873" alt="image" src="https://github.com/user-attachments/assets/06f19f6f-8001-4a70-87b2-1904c5db698e" />
I implemented a recursive method checkDigits that checks whether all characters in a string are digits. In main, 
I took a string input from the user, called the method, and printed "Yes" if all characters are digits, otherwise "No".
<img width="1540" height="873" alt="image" src="https://github.com/user-attachments/assets/a99a3839-af08-420b-bd7c-eaa3e03476d3" />
I implemented a recursive method countCharacters that calculates the number of characters in a string. In main, 
I took a word as input from the user, called the method, and printed the total number of characters.
<img width="1540" height="873" alt="image" src="https://github.com/user-attachments/assets/e4bf3f73-f356-4092-bec4-3fb6a6090ae0" />
I implemented a recursive method findGCDRecursive based on the Euclidean algorithm to find the greatest common divisor of two numbers.
In main, I read two integers from the user, called the method, and printed the result.
